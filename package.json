{
  "name": "sexhouseisland",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight, vibe-coded LucasArts-style point-and-click adventure engine (vertical slice).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen": "tsx scripts/generate-sprite.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
